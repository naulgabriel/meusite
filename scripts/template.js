// JavaScript Document
$( document ).ready(function() {
	// Handler for .ready() called.
	$.Footer({
		element : '#rodape',
		setWidth : false
	});	
	
	$('.collapse').collapse();
	
	 $('.jcarousel').jcarousel({
        // Configuration goes here
    });
		

});
